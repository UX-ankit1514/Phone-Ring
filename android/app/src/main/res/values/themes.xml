<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.ArnifiPhoneBell" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:fontFamily">sans</item>
        <item name="android:colorAccent">#006C4C</item>
        <item name="android:navigationBarColor">#F5F7F5</item>
        <item name="android:statusBarColor">#F5F7F5</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowActionModeOverlay">true</item>
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
    </style>

    <!--
        The incoming-request screen paints its own scrim over whatever is behind it, so the
        window itself stays transparent and undimmed; adding backgroundDim on top would
        darken the card twice. windowDisablePreview keeps a blank frame from flashing when
        the screen is turned on to show the caller.
    -->
    <style name="Theme.ArnifiPhoneBell.Incoming" parent="Theme.ArnifiPhoneBell">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
