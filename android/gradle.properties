org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
android.useAndroidX=true
android.nonTransitiveRClass=true

# Supply client values through the gitignored firebase.properties file,
# ~/.gradle/gradle.properties, or -P arguments. Never commit enrollment codes,
# release keystores, or signing secrets.

# Production release signing values belong in ~/.gradle/gradle.properties or
# the approved CI secret store.
# ARNIFI_KEYSTORE_PATH=/absolute/path/to/arnifi-phone-bell.jks
# ARNIFI_KEYSTORE_PASSWORD=
# ARNIFI_KEY_ALIAS=
# ARNIFI_KEY_PASSWORD=
